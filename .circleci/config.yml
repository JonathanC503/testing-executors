version: 2.1

jobs:
  docker-test:
    docker:
      - image: circleci/openjdk:8u171-jdk
      - image: httpd
    steps:
      - checkout
      - run: 
         command:  | 
            echo This step runs in the openjdk container
      - run: sleep 5 && nc -v localhost 81

workflows:
  run-executors:
    jobs: 
      - docker-test
  
